// Placeholder — generated types can replace this with:
// `supabase gen types typescript --local > types/database.ts`
// In the meantime, we type table rows via `@/types/domain` on the call-site.
export type Database = Record<string, unknown>;
