---
tags: [session, handoff]
created: 2026-04-29
---

# 2026-04-29

## Summary

- Cloned `MainTez/Valorant` locally and loaded the brain context.
- Added `/api/schedule-events` with `POST`, `PATCH`, and `DELETE`, gated by `requireCoachOrAdmin()`.
- Validated schedule participants against the current team before writing `schedule_events`.
- Added the `/calendar` create/edit/delete dialog for coaches and admins, including kind, start/end, location, description, and participants.
- Fixed `/players` roster cards using stale rank/tracker data by adding a shared Henrik profile sync helper and refreshing linked Riot profiles before rendering cards.
- Reused the shared sync helper from `/api/cron/refresh-stats` so roster sync and cron sync write profile + tracked stat data through one path.
- Started the Windows desktop-app path: Electron shell, tray auto-start, `/desktop` feed, `/desktop/overlay`, `match_moments`, desktop sync APIs, and deterministic savage match callouts.
- Added public `/download` page, a stable GitHub Releases installer URL, and a `Desktop Release` GitHub Action that builds `Nexus-Team-Hub-Setup.exe`.
- Packaged desktop builds now default to `https://molgarians-premier-hub.vercel.app` while local Electron dev still defaults to `http://localhost:3000`.
- Updated [[status]], [[next-up]], and [[index]] so the calendar roadmap item is no longer stale.

## Verification

- `npm ci`
- `npm test`
- `npm run lint`
- `npm run type-check`
- `npm run build`
- `node --check desktop/main.cjs`
- `node --check desktop/preload.cjs`
- `npx electron --version`
- `npx electron-builder --dir --win --x64`

## Notes

- `npm ci` reported 2 audit issues: 1 moderate and 1 critical. No forced audit fix was applied because `npm audit fix --force` can introduce breaking dependency changes.
- The first remaining roadmap items still need live Supabase/Vercel credentials before they can be completed.
